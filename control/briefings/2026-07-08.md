# Daily Briefing — 2026-07-08

**Today**
Launch is ~2 days out (target 2026-07-10). Kairo repo is clean, sitting on `fix/signout-delete-account-flow`, two open draft PRs waiting on you: #21 (sign out/delete account) and #20 (dev-client setup). No ⚠️ blocked items in the queue.

**Done since yesterday**
- Fixed the live `/terms` 404 on the marketing site — turned out to be a stale local deploy, not a code bug; pulled + redeployed, verified live.
- PR #21: fixed two real bugs — sign-out hanging on deep screens (added an app-wide auth gate), and delete-account leaving orphaned profile data (added a hardened SQL migration + centralized local-state wipe). Both verify.sh gates pass.
- Reminder: PR #18 (dark mode) and #19 (TestFlight reveal pull-forward) are already merged to main, so nothing pending there anymore.

**Waiting on you**
- Run migration `009_account_deletion_hardening.sql` by hand in the Supabase SQL Editor (needed for PR #21's fix to actually take effect), then test sign-out + delete-account on-device.
- Pre-launch checklist (all still open): Kairo prompt order, shareable postcard, reveal flow, remove test data/accounts, notifications, webpage, reset last week's Kairo, wire "invite a friend" once App Store listing is live.
- TestFlight screenshots + pasting listing copy into App Store Connect.
- Buy a domain for Kairo and hand it to me to wire up in Vercel.
- Gmail/Calendar are connected but need a permission grant I can't approve from here — skipped for today.

**Suggestion**
Run migration 009 + do the on-device sign-out/delete test first — it's the one item blocking a merge (PR #21) and it's a real data-integrity bug (partial account wipe), which you don't want live when App Store users start deleting accounts in ~2 days.

BRIEF: Kairo: PR #21 (signout/delete fix) ready — run migration 009 + test on device; pre-launch checklist still open, launch is ~2 days out.
